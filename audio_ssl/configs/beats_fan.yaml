extends: beats_jepa.yaml

# BEATs continued-SSL on fans only (matches the jepa_fan / lejepa_fan experiments).
data:
  machines: [fan]

output:
  directory: audio_ssl/outputs/beats_fan

logging:
  comet:
    tags: [beats, jepa, mimii, pretrained, fan-only]
